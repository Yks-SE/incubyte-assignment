{
  "name": "incubyte-assignment",
  "version": "1.0.0",
  "scripts": {
    "test": "cypress run --spec \"cypress/e2e/features/userFlow/userFlow.feature\""
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^15.1.5",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.5",
    "@cypress/browserify-preprocessor": "^3.0.2",
    "cypress": "^12.17.4",
    "esbuild": "^0.19.12"
  }
}
